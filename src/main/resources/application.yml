---
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/CognizantFSE/spring-config-server
          searchPaths: music-player
server:
  port: 8888
